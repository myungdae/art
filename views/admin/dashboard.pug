extends ../layout

block content
  .container.mt-5
    h2.text-center.mb-4 Admin Dashboard

    // ✅ Employers
    h4.mb-3.text-primary Employers
    table.table.table-bordered.table-striped
      thead
        tr
          th Username
          th Email
          th Remaining Tokens
          th Created At
      tbody
        each emp in employers
          tr
            td= emp.username || 'N/A'
            td= emp.email || 'N/A'
            td= emp.adsAvailable || 0
            td= emp.createdAt ? new Date(emp.createdAt).toLocaleDateString() : 'N/A'

    // ✅ Job Seekers
    h4.mt-5.mb-3.text-success Job Seekers
    table.table.table-bordered.table-striped
      thead
        tr
          th Username
          th Email
          th Remaining Days
          th Created At
      tbody
        each js in jobSeekers
          tr
            td= js.name || 'N/A'
            td= js.email || 'N/A'
            td= js.remainingDays || 0
            td= js.createdAt ? new Date(js.createdAt).toLocaleDateString() : 'N/A'

    // ✅ Online Tutors
    h4.mt-5.mb-3.text-info Online Tutors
    table.table.table-bordered.table-striped
      thead
        tr
          th Username
          th Email
          th Remaining Days
          th Created At
      tbody
        each ot in onlineTutors
          tr
            td= ot.name || 'N/A'
            td= ot.email || 'N/A'
            td= ot.remainingDays || 0
            td= ot.createdAt ? new Date(ot.createdAt).toLocaleDateString() : 'N/A'
