extends ../layout

block content
  h2 Admin Dashboard
  table.table
    thead
      tr
        th Title
        th Actions
    tbody
      each job in jobs
        tr
          td= job.title
          td
            form(method="POST", action=`/admin/delete/${job._id}`)
              button.btn.btn-danger(type="submit") Delete
